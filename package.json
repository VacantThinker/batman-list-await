{
  "name": "batman-list-await",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "export": "rmdir /q /s .next && next build && next export ",
    "deploy": "npm run export && type nul > out/.nojekyll && gh-pages -t -d out",
    "start": "next build && next start"
  },
  "dependencies": {
    "isomorphic-unfetch": "^3.0.0",
    "next": "9.1.2",
    "react": "16.11.0",
    "react-dom": "16.11.0"
  },
  "devDependencies": {
    "axios": "^0.19.0",
    "gh-pages": "^2.1.1",
    "prettier": "^1.18.2"
  }
}
